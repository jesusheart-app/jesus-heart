rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 앱 데이터는 로그인한 교인만 접근할 수 있습니다.
    match /appdata/{docId} {
      allow read, write: if request.auth != null;
    }
  }
}
